import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
	mainBorder: {
		flex: 1,
		backgroundColor: 'red',
	},
	textPreview: {
		textAlign: 'center',
	},
});

export default styles;
